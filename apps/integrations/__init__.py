"""REST API integration layer for BrightBean Studio.

Provides token-authenticated API endpoints so external tools (e.g. Hermes) can
create and manage social media posts programmatically. All endpoints are
prefixed under /api/v1/.
"""
